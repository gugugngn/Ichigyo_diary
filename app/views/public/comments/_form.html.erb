<div id ="comments-error"></div>
<%= form_with(model:[post, comment], local: false) do |f| %>
  <%= f.text_area :body, rows:'3',placeholder: "コメントをここに", class: "comment_form" %>
  <%= f.submit "送信" %>
<% end %>