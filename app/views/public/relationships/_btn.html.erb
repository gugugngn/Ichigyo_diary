<!--フォローするボタンとフォロー外すボタンの表示-->

<% if current_user.following?(user) %>
  <%= link_to "フォローを外す", user_relationships_path(user), method: :delete, class: "btn btn-outline-dark" %>
<% else %>
  <%= link_to "フォローする",user_relationships_path(user),method: :post, class: "btn btn-outline-dark" %>
<% end %>