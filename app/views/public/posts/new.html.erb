<div class='container'>
  <div class='row'>
    <div class="mx-auto">
      <h1>新規投稿画面</h1>
        <%= render "layouts/errors", obj: @post %>
        <p><%= @current_date.strftime("%-m月%-d日") %></p>
        <%= render 'form', post: @post, url: posts_path %>
    </div>
  </div>
</div>