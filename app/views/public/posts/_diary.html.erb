<div class="post-box">
  <div class="mood-details">
    <% moods.each do |mood| %>
      <div class="mood-item" style="color: <%= mood.color %>;">
        <h5 class="mood-name">
          <%= mood.name %>
        </h5>
      </div>
    <% end %>
  </div>
  <div class="mood-container" style="background: <%= post.background_color %>;">
    <div class="post-body">
      <h5><%= l post.created_at, format: :long %>の<%= post.user.name %>さんの１行日記</h5>
      <h5><%= post.body %></h5>
    </div>
  </div>
</div>

<!--renderで引っ張る際はmoodsとpostに変数を渡す-->