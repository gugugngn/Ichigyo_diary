<!DOCTYPE html>
<html>
  <head>
    <title>IchigyoDiary</title>
    <meta name="viewport" content="width=device-width,initial-scale=1">
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>
    <script src="https://kit.fontawesome.com/76f4db3613.js" crossorigin="anonymous"></script>
    <%= stylesheet_pack_tag 'application', media: 'all', 'data-turbolinks-track': 'reload' %>
    <%= javascript_pack_tag 'application', 'data-turbolinks-track': 'reload' %>
  </head>

  <body>
    <header class="mx-auto text-center border-bottom list-unstyled">
      <% if user_signed_in? %>
        <% if current_user.guest_user? %>
          <!-- ゲストユーザー用のリンク -->
          <li>
            <%= link_to "ログアウト", destroy_user_session_path, method: :delete %>
          </li>
          <li>
            <%= link_to "みんなの１行", posts_path %>
          </li>
        <% else %>
          <!-- ログインユーザー用のリンク -->
          <li>
            <%= link_to "ログアウト", destroy_user_session_path, method: :delete %>
          </li>
          <li>
            <%= link_to "みんなの１行", posts_path %>
          </li>
          <li>
            <%= link_to "マイページ", mypage_user_path(current_user) %>
          </li>
          <li>
            <%= link_to edit_user_path(current_user) do %>
              <i class="fa-solid fa-gear"></i> プロフィール編集
            <% end %>
          </li>
        <% end %>
      <% else %>
      <!-- ログアウト状態のリンク -->
      <li>
        <%= link_to "新規登録", new_user_registration_path %>
      </li>
      <li>
        <%= link_to "ログイン", new_user_session_path %>
      </li>
    <% end %>
    </header>
    <main>
      <% flash.each do |message_type, message| %>
        <div class="flash-message <%= message_type %>"><%= message %></div>
      <% end %> 
    </main>
    <%= yield %>
  </body>
</html>
